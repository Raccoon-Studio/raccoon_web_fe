import { JSX } from "react";
import { PagePlaceholder } from "@/components/dashboard/page-placeholder";

export default function TaskPage(): JSX.Element {
  return (
    <PagePlaceholder
      title="task screen"
      subtitle="Track your daily tasks and active assignments."
    />
  );
}
